#!/bin/bash

set -e
version="5.0.1"

cd $JB_CT_SRC
tar -xvf gmp-${version}.tar.bz2 && cd gmp-${version}
CPPFLAGS=-fexceptions ./configure \
    --prefix=${JB_CROSS_TOOLS}
make
make check
make install

# Clean up build dir
cd $JB_CT_SRC && rm -rf gmp-${version}
