#!/bin/bash

# Install the linux headers on the rootfs

cd $JB_SRC/linux
make mrproper
make ARCH=${JB_ARCH} headers_check
make ARCH=${JB_ARCH} INSTALL_HDR_PATH=dest headers_install
cp -rv dest/include/* ${JB_ROOT_FS}/usr/include
 
